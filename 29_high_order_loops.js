// for of 
// these are array specific and  object loops 
// in for loop the object may be string array or object dont take it only as an object 
// for (const element of object) {
    
// }

const greetings = "helllo there!"

for (const greet of greetings) {
    console.log(`each char is ${greet}`);
    
}